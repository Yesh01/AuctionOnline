import javax.swing.JFileChooser;
import java.io.File;

public class Main {
	
	public static void main(String[] args)
	{
		/*
		try {
            // Create an instance of ImageChooser
            ImageChooser imageChooser = new ImageChooser();

            // Choose an image file
            File selectedFile = imageChooser.chooseImageFile();

            // Check if a file was selected
            if (selectedFile != null) {
                System.out.println("Selected file: " + selectedFile.getAbsolutePath());
            } else {
                System.out.println("No image file selected.");
            }
        } catch (Exception e) {
            System.err.println("An error occurred: " + e.getMessage());
        }
        */
		
		String loco = "(VIP) Custer";
		
		System.out.println(loco.contains("(VIP)"));
	//	AppWindow auctionMainScreen = new AppWindow();
	//	auctionMainScreen.frmAuctionSystem.setVisible(true);
		
		//auctionMainScreen.frmAuctionSystem.btnNewButton_1
	}
}
